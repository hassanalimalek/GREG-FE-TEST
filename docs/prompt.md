Create a modern, responsive landing page using the Web Awesome framework.

FRAMEWORK REQUIREMENT:
- You MUST use Web Awesome (https://webawesome.com) for all UI components
- Use Web Awesome's color variables, components, and patterns
- Include the Web Awesome CDN in your HTML

PROJECT REQUIREMENTS:
Create a landing page for a "Digital Agency" with these sections:
1. Hero section (headline, subheading, CTA button)
2. Services section (3-4 service cards in a grid)
3. Portfolio/Projects section (image gallery or cards)
4. Contact section (simple contact info with icons)

MANDATORY BROWSER APIs TO USE:
1. Popover API - use for a tooltip or info popup
2. Container queries - for responsive card layouts
3. :has() pseudo-selector - for parent element styling based on child state

CSS REQUIREMENTS:
- Use modern CSS Grid and Flexbox
- Implement responsive design with media queries
- Use Web Awesome's color 0variables (custom properties)
- NO outdated practices (no floats for layout, no table layouts, etc.)
- Mobile-first approach

IMAGE REQUIREMENTS:
- Use SVG icons throughout (from Web Awesome icon set or inline SVG)
- At least 3-4 SVG images
- Explain in comments how to optimize/compress SVGs

CODE QUALITY:
- Clean, well-organized code
- Commented explanations for key decisions
- Semantic HTML5
- Accessibility considerations (ARIA labels where needed)
- Modern JavaScript (ES6+) if needed

LIBRARIES:
- Web Awesome (required) - only
- No jQuery or deprecated libraries
- Specify versions if using CDN

DELIVERABLES:
1. Complete HTML file(s)
2. CSS file (can be inline or separate)
3. JavaScript if needed but avoid
4. Comments explaining:
   - How you used the popover API
   - Where you implemented container queries
   - Where you used :has() selector
   - SVG optimization approach

Please provide complete, working code that I can copy and run immediately.